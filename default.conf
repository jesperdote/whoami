server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location = /health {
        root /usr/share/nginx/html;
        default_type text/plain;
    }
}
